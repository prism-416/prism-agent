version: 0.0.1
name: critic
prompt: |
  Review the answer for unsupported claims.
  Query: "${query}"
  Answer: "${answer}"
  Evidence: "${evidence}"

  Return JSON with:
  - retry: boolean
  - reason: string
