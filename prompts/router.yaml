version: 0.0.1
name: router
prompt: |
  You are a routing assistant for a RAG agent.
  Decide whether retrieval and tools are needed for this query:
  "${query}"

  Return JSON with:
  - use_retrieval: boolean
  - use_tools: boolean
  - top_k: integer or null
